<% var page = letter.page() %>
<% var character = letter.character()  %>
<% var nextPage = letter.nextPage() %>
<% var beforePage = letter.beforePage()%>
<h1>Letter <%= character.toUpperCase() %></h1>
<h2>Page <%= page %></h2>

<% if (nextPage) { %>
  <% var url = `/letter=${character}/page=${page + 1 }` %>
  <a href="<%= url %>">Next</a>
<% } %>
